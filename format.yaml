services:
  - name: my-node-service
    type: node
    resources:
      memory: 1GB
      cpu: 500m
    scaling:
      enabled: true
      min: 1
      max: 5
    env:
      - key: NODE_ENV
        value: production
